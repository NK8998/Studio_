import withTransition from "../../Utilities/transition";

const Analytics = ({names})=>{

    console.log(names)
 
    return(
       <div className="analytics">
       
       </div> 
    )
}

export default withTransition(Analytics);